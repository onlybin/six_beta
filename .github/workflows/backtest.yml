name: 🔬 独立量化回测引擎 (Backtest)

on:
  workflow_dispatch: # 仅允许手动触发，不会自动定时运行，不干扰主程序

jobs:
  run-backtest-engine:
    runs-on: ubuntu-latest

    steps:
      - name: 拉取最新代码
        uses: actions/checkout@v3

      - name: 配置 Python 环境 (3.10)
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 安装依赖库 (含深度学习框架)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 执行历史步进回测 (Walk-Forward Validation)
        run: |
          python backtest.py
